{
  "name": "eduonix",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "*",
    "cheerio": "*",
    "copyfiles": "*",
    "create-react-class": "*",
    "react": "*",
    "react-bootstrap": "*",
    "react-dom": "*",
    "react-scripts": "*"
  },
  "scripts": {
    "start": "react-scripts build && copy build server/build",
    "build": "react-scripts build && copy build server/build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-less": "^1.4.1",
    "grunt-contrib-watch": "^1.0.0",
    "less-plugin-autoprefix": "^1.5.1",
    "less-plugin-clean-css": "^1.5.1"
  }
}
